[tool.poetry]
name = "botholiday"
version = "0.1.0"
description = ""
authors = ["Sinderella"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.28.1"
icalendar = "^5.0.4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
